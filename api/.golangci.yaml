version: '2'

linters:
  default: all
  disable:
    - cyclop
    - depguard
    - err113
    - exhaustruct
    - exhaustruct_v5
    - goconst
    - gomodguard
    - misspell
    - mnd
    - varnamelen
    - wrapcheck
    - wsl
    - dupl
  settings:
    funlen:
      lines: 200
      statements: 120
    revive:
      rules:
        - name: exported
          disabled: true
        - name: package-comments
          disabled: true
  exclusions:
    rules:
      - path: (^|.*[\\/])(mongo|redis)[\\/]
        linters:
          - tagliatelle
        text: "json\\(camel\\): got '_id' want 'id'"

formatters:
  enable:
    - gofumpt
    - goimports
  settings:
    gofumpt:
      module-path: github.com/rafinhacuri/SanchezDNS/api
      extra:
        group-params: true
    goimports:
      local-prefixes:
        - github.com/rafinhacuri/SanchezDNS/api
